# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.Python

# Virtual environments
venv/
.venv/
env/
.env/
ENV/

# Runtime index artifacts (generated by the indexer)
.code-index/
*.faiss
doc_store.json
last_indexed_commit.txt

# SQLite
*.db
*.db-shm
*.db-wal

# Model weights and cache (downloaded at runtime)
.cache/
models/
*.bin
*.safetensors
*.pt

# IDE / Editor
.idea/
.vscode/
*.sublime-*
*.sw?
.DS_Store
Thumbs.db

# Claude Code (local settings — not project configuration)
.claude/settings.local.json

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Graphify trial artifacts — the tool was removed (ADR-006 builds this natively).
# Keep only the human-readable reports for reference; ignore all machine data/state/cache.
graphify-out/*
!graphify-out/GRAPH_REPORT.md
!graphify-out/graph.html
