# Core indexing and embeddings
faiss-cpu
sentence-transformers
transformers
numpy

# AST parsing
tree-sitter>=0.21
tree-sitter-python
tree-sitter-typescript
tree-sitter-javascript
tree-sitter-c-sharp
tree-sitter-cpp

# Graph analytics — community detection + centrality (ADR-006)
networkx>=3.0

# Optional, faster/higher-quality community detection (Leiden). Not required —
# graph_analytics falls through to NetworkX Louvain when these are absent.
# Native build deps; install only if you want the Leiden backend:
#   pip install leidenalg python-igraph
# leidenalg
# python-igraph

# MCP server
mcp[cli]

# File-system change monitoring (optional — enables auto-reindex)
watchdog

# Terminal UI
textual
rich

# Risk rules engine (H6 — rules.yaml support)
pyyaml
